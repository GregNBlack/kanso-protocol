name: Deploy Storybook
on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

# Serialize Pages deploys — newest commit wins. cancel-in-progress was
# `false` originally, but two pushes in quick succession then deadlocked
# the queued deploy against GitHub's environment lock and the second one
# kept reporting `failure`. Latest-wins is fine for a docs site.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'
      - run: npm ci --legacy-peer-deps
      - name: Build tokens
        run: npm run build:tokens
      - name: Build Storybook
        run: npx ng run kanso-protocol:build-storybook
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: storybook-static

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

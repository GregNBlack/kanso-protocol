---
"@kanso-protocol/ui": minor
---

**UserMenu** — new `[presence]` input drives the avatar's presence dot (`online` / `away` / `busy` / `offline`, or `null` to hide it). Previously the dot was hard-coded to `online`. Resolves the user-menu open question → promoted `beta → stable` (patterns now 15 stable · 5 beta).

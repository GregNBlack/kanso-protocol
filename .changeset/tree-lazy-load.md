---
"@kanso-protocol/ui": minor
---

**Tree** — lazy-load contract for expandable nodes. Expanding an `expandable` node that has no loaded `children` emits `(nodeExpand)`; set the node's `loading` flag while fetching and the tree shows a loading row until `children` is populated. Resolves the tree open question → promoted `beta → stable` (now 38 stable · 2 beta components).

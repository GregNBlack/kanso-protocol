---
"@kanso-protocol/ui": minor
---

**PageHeader** — new `[collapsed]` compact mode that hides the breadcrumbs and description rows (keeping the title, actions, and tabs). Pair it with your own scroll listener for a shrink-on-scroll header, or toggle it explicitly. Resolves the page-header open question → promoted `beta → stable` (patterns now 17 stable · 3 beta).
